rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /wizardProgress/{platformId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /checkIns/{dateId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /usageLogs/{dateId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
